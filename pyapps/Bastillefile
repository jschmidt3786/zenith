INCLUDE zenith/base

PKG tiff openjpeg freetype lcms2 webp tcl87 tk87 harfbuzz fribidi libraqm libimagequant libxcb 
PKG python38 py38-virtualenv py38-pip py38-pycryptodome py38-requests py38-xmltodict py38-pillow
PKG py38-tkinter

CMD pw groupadd -n dce -g XXX
CMD pw useradd dce -c "(j)dce service" -g dce -s sh -m -L daemon -u XXX
CMD pw groupadd -n portal -g 10000 -M dce
# TODO: needs work here; for guidebuilder, gem...

# repo config for in-house pyapps packages (home/office/cloud)
CP usr /
